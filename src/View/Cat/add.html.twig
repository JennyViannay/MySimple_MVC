{% extends 'layout.html.twig' %}

{% block content %}
    <form method="post">
        <input type="hidden" value="{{ cat.id }}" name="id">
        <input type="text" value="{{ cat.name }}" name="name">
        <input type="text" value="{{ cat.img }}" name="img">
        <input type="submit" value="Valider">
    </form>
{% endblock %}
